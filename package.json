{
  "name": "command-line-sendkeys",
  "version": "1.0.0",
  "description": "A simple Node.js script to send keystrokes from the command line using the `sendkeys` package.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js -s 1000 -r 3 Venamoos"
  },
  "author": "venamoos",
  "license": "WTFPL",
  "dependencies": {
    "sendkeys": "^1.1.1"
  }
}
